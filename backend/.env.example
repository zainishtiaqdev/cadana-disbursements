# Copy to .env for the Postgres path. Leave DATABASE_URL unset to use the
# in-memory store (the default — a zero-setup, one-command local run).
# DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/postgres
PORT=8080
ALLOWED_ORIGIN=*
